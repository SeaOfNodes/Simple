{i32 i64 u32 -> u32} write="C";
val _p={u8[~] str -> i64 ptr=str; return write(1,ptr,str#);};
int a = 1;
int b = 1;

int x = -1;
int y=1;
int z= -1;

int g = x++ || y++ || z++;

int switch = 0;
if(x == 4 || y == 4 || z == 4) {
    switch = -1;
} else {
    switch = 1;
}
int cd = 1;
if((x == 0 && y == 1 && z==-1) && switch && g) {
    _p("Effected");
} else {
    _p("Not effected");
}
return 0;
