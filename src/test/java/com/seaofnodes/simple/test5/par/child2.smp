// -*- mode: java;  -*-

// Child module

// Same field name as child2
val kid1 = 11;

// Can read parent directly.
// Can read defined-in-parent fields.
val kid2 = child0.kid0;

// OK to reference a past sibling
val kid3 = child1.kid1;
