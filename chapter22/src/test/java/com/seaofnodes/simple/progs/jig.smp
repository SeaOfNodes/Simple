// -*- mode: java;  -*-
// theory: conservative approx to a CG will link all target fcsn.
// unlinked *local* fcns with *no* TFP constants can be declared unreachable and removed.

// Newtons approximation to the square root
val sqrt = { int x ->
    int guess = x;
    while( 1 ) {
        int next = (x/guess + guess)/2;
        if( next == guess ) return guess;
        guess = next;
    }
};
int cast_int = arg+2;
return sqrt(arg) + sqrt(cast_int);
